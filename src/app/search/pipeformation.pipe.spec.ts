import { PipeformationPipe } from './pipeformation.pipe';

describe('PipeformationPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeformationPipe();
    expect(pipe).toBeTruthy();
  });
});
